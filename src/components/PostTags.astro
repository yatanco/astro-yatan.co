---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex gap-2">
  {tags.map((tag) => (
    <a
      href={`/tags/${tag}`}
      class="text-sm px-3 py-1 text-primary-600 hover:text-primary-500 bg-gray-100 dark:text-primary-500 dark:bg-gray-800 rounded-full hover:bg-gray-200 dark:hover:bg-gray-700"
    >
      #{tag}
    </a>
  ))}
</div>
